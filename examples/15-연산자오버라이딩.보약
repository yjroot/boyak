## 연산자 오버라이딩 예제 ##

# 2차원 벡터 클래스
틀 벡터:
    생성(엑스, 와이):
        자신의 엑스는 엑스이다
        자신의 와이는 와이이다

    # 벡터 덧셈 (+)
    방법 더하기(다른벡터):
        새엑스는 자신의 엑스 + 다른벡터의 엑스이다
        새와이는 자신의 와이 + 다른벡터의 와이이다
        벡터(새엑스, 새와이)를 돌려주라

    # 벡터 뺄셈 (-)
    방법 빼기(다른벡터):
        새엑스는 자신의 엑스 - 다른벡터의 엑스이다
        새와이는 자신의 와이 - 다른벡터의 와이이다
        벡터(새엑스, 새와이)를 돌려주라

    # 스칼라 곱셈 (*)
    방법 곱하기(배수):
        새엑스는 자신의 엑스 * 배수이다
        새와이는 자신의 와이 * 배수이다
        벡터(새엑스, 새와이)를 돌려주라

    # 벡터 동등 비교 (==)
    방법 같음(다른벡터):
        자신의 엑스 == 다른벡터의 엑스 그리고 자신의 와이 == 다른벡터의 와이를 돌려주라

    # 벡터 출력
    방법 문자열():
        "({자신의 엑스}, {자신의 와이})"를 돌려주라


# 테스트
"=== 벡터 연산자 오버라이딩 ==="을 출력하라

벡터1은 벡터(3, 4)이다
벡터2는 벡터(1, 2)이다

"벡터1: {벡터1의 문자열()}"을 출력하라
"벡터2: {벡터2의 문자열()}"을 출력하라

# 덧셈 테스트
합은 벡터1 + 벡터2이다
"벡터1 + 벡터2 = {합의 문자열()}"을 출력하라

# 뺄셈 테스트
차는 벡터1 - 벡터2이다
"벡터1 - 벡터2 = {차의 문자열()}"을 출력하라

# 스칼라 곱셈 테스트
곱은 벡터1 * 2이다
"벡터1 * 2 = {곱의 문자열()}"을 출력하라

# 동등 비교 테스트
벡터3은 벡터(3, 4)이다
"벡터1 == 벡터3: {벡터1 == 벡터3}"을 출력하라
"벡터1 == 벡터2: {벡터1 == 벡터2}"을 출력하라

""을 출력하라

# 분수 클래스 예제
틀 분수:
    생성(분자, 분모):
        자신의 분자는 분자이다
        자신의 분모는 분모이다

    # 분수 덧셈
    방법 더하기(다른분수):
        새분자는 자신의 분자 * 다른분수의 분모 + 다른분수의 분자 * 자신의 분모이다
        새분모는 자신의 분모 * 다른분수의 분모이다
        분수(새분자, 새분모)를 돌려주라

    # 분수 곱셈
    방법 곱하기(다른분수):
        새분자는 자신의 분자 * 다른분수의 분자이다
        새분모는 자신의 분모 * 다른분수의 분모이다
        분수(새분자, 새분모)를 돌려주라

    # 분수 동등 비교
    방법 같음(다른분수):
        자신의 분자 * 다른분수의 분모 == 다른분수의 분자 * 자신의 분모를 돌려주라

    # 분수 출력
    방법 문자열():
        "{자신의 분자}/{자신의 분모}"를 돌려주라


"=== 분수 연산자 오버라이딩 ==="을 출력하라

반은 분수(1, 2)이다
삼분의일은 분수(1, 3)이다

"1/2: {반의 문자열()}"을 출력하라
"1/3: {삼분의일의 문자열()}"을 출력하라

# 분수 덧셈 (1/2 + 1/3 = 5/6)
합분수는 반 + 삼분의일이다
"1/2 + 1/3 = {합분수의 문자열()}"을 출력하라

# 분수 곱셈 (1/2 * 1/3 = 1/6)
곱분수는 반 * 삼분의일이다
"1/2 * 1/3 = {곱분수의 문자열()}"을 출력하라

# 분수 동등 비교
다른반는 분수(2, 4)이다
"1/2 == 2/4: {반 == 다른반}"을 출력하라

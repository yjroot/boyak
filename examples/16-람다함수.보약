## 람다 함수 예제 ##

# 기본 람다 - 단일 매개변수
"=== 기본 람다 ===" 을 출력하라
곱하기2는 숫자 -> 숫자 * 2이다
"곱하기2(5) = {곱하기2(5)}" 를 출력하라
"곱하기2(10) = {곱하기2(10)}" 를 출력하라

# 다중 매개변수 람다
"=== 다중 매개변수 ===" 을 출력하라
더하기는 (값1, 값2) -> 값1 + 값2이다
"더하기(3, 7) = {더하기(3, 7)}" 를 출력하라

곱하기는 (값1, 값2) -> 값1 * 값2이다
"곱하기(4, 5) = {곱하기(4, 5)}" 를 출력하라

# 매개변수 없는 람다
"=== 매개변수 없는 람다 ===" 을 출력하라
상수값는 () -> 42이다
"상수값() = {상수값()}" 를 출력하라

# 클로저 - 외부 변수 캡처
"=== 클로저 ===" 을 출력하라
배수만들기를 정의하자 배수를 받아
    숫자 -> 숫자 * 배수를 돌려주라

두배는 배수만들기(2)이다
세배는 배수만들기(3)이다
다섯배는 배수만들기(5)이다

"두배(7) = {두배(7)}" 를 출력하라
"세배(7) = {세배(7)}" 를 출력하라
"다섯배(7) = {다섯배(7)}" 를 출력하라

# 람다를 함수 인수로 전달
"=== 고차 함수 ===" 을 출력하라
적용하기를 정의하자 함수와 값을 받아
    함수(값)을 돌려주라

결과는 적용하기(숫자 -> 숫자 + 100, 5)이다
"적용하기(숫자 -> 숫자 + 100, 5) = {결과}" 를 출력하라

# 조건부 람다 반환
"=== 조건부 람다 반환 ===" 을 출력하라
연산자선택를 정의하자 종류를 받아
    만약 종류 가 "더하기" 와 같으면
        (값1, 값2) -> 값1 + 값2를 돌려주라
    아니면 종류 가 "곱하기" 와 같으면
        (값1, 값2) -> 값1 * 값2를 돌려주라
    그외에
        (값1, 값2) -> 값1 - 값2를 돌려주라

더하기함수는 연산자선택("더하기")이다
곱하기함수는 연산자선택("곱하기")이다

"더하기함수(10, 3) = {더하기함수(10, 3)}" 를 출력하라
"곱하기함수(10, 3) = {곱하기함수(10, 3)}" 를 출력하라


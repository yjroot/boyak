## 튜플과 다중 할당 예제 ##

# 튜플 리터럴
"=== 튜플 리터럴 ===" 을 출력하라
좌표는 (10, 20)이다
"좌표: {좌표}" 를 출력하라

삼원색은 (255, 128, 0)이다
"RGB: {삼원색}" 을 출력하라

# 다중 할당 (튜플 언패킹)
"=== 다중 할당 ===" 을 출력하라
값1, 값2는 (100, 200)이다
"값1 = {값1}" 을 출력하라
"값2 = {값2}" 를 출력하라

# 리스트로도 언패킹 가능
이름, 나이, 도시는 ["홍길동", 25, "서울"]이다
"이름: {이름}, 나이: {나이}, 도시: {도시}" 를 출력하라

# 함수에서 튜플 반환
"=== 함수에서 튜플 반환 ===" 을 출력하라
좌표만들기를 정의하자 엑스와 와이를 받아
    (엑스, 와이)를 돌려주라

결과는 좌표만들기(30, 40)이다
"좌표: {결과}" 를 출력하라

# 함수 반환값 직접 언패킹
엑스, 와이는 좌표만들기(50, 60)이다
"x = {엑스}, y = {와이}" 를 출력하라

# 여러 값 반환하는 함수
"=== 여러 값 반환 ===" 을 출력하라
나눗셈를 정의하자 피젯수와 젯수를 받아
    몫은 피젯수 // 젯수이다
    나머지는 피젯수 % 젯수이다
    (몫, 나머지)를 돌려주라

몫값, 나머지값은 나눗셈(17, 5)이다
"17 / 5 = 몫: {몫값}, 나머지: {나머지값}" 을 출력하라

# 튜플 중첩
"=== 중첩 튜플 ===" 을 출력하라
점들은 ((0, 0), (10, 10), (20, 5))이다
"점들: {점들}" 을 출력하라

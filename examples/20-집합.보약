## 집합(Set) 예제 ##

# 집합 생성
"=== 집합 생성 ===" 을 출력하라
숫자집합은 집합([1, 2, 3, 4, 5])이다
"숫자 집합: {숫자집합}" 을 출력하라

# 중복 제거
"=== 중복 제거 ===" 을 출력하라
중복목록은 [1, 2, 2, 3, 3, 3, 4, 4, 4, 4]이다
"원본 목록: {중복목록}" 을 출력하라
유일값은 집합(중복목록)이다
"중복 제거: {유일값}" 을 출력하라

# 집합 연산
"=== 집합 연산 ===" 을 출력하라
집합1은 집합([1, 2, 3, 4, 5])이다
집합2는 집합([4, 5, 6, 7, 8])이다
"집합1: {집합1}" 을 출력하라
"집합2: {집합2}" 를 출력하라

# 합집합
합은 합집합(집합1, 집합2)이다
"합집합: {합}" 을 출력하라

# 교집합
교는 교집합(집합1, 집합2)이다
"교집합: {교}" 을 출력하라

# 차집합
차는 차집합(집합1, 집합2)이다
"차집합 (집합1 - 집합2): {차}" 를 출력하라

# 대칭 차집합
대칭은 대칭차집합(집합1, 집합2)이다
"대칭차집합: {대칭}" 을 출력하라

# 문자열 집합
"=== 문자열 집합 ===" 을 출력하라
과일들은 집합(["사과", "배", "포도", "사과", "딸기", "배"])이다
"과일 종류: {과일들}" 을 출력하라
"과일 수: {길이(과일들)}" 를 출력하라

# 포함 확인
"=== 포함 확인 ===" 을 출력하라
숫자들은 집합([1, 2, 3, 4, 5])이다
만약 3 가 숫자들에 있으면
    "3은 집합에 있습니다" 를 출력하라
만약 10 가 숫자들에 없으면
    "10은 집합에 없습니다" 를 출력하라

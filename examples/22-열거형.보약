## 열거형(Enum) 예제 ##

# 기본 열거형 정의
"=== 기본 열거형 ===" 을 출력하라

색상 열거형을 정의하자
    빨강
    파랑
    초록
    노랑

"색상 열거형: {색상}" 을 출력하라
"빨강: {색상.빨강}" 을 출력하라
"파랑: {색상.파랑}" 을 출력하라
"초록: {색상.초록}" 을 출력하라

# 값이 지정된 열거형
"=== 값이 지정된 열거형 ===" 을 출력하라

방향 열거형을 정의하자
    북은 0이다
    동은 90이다
    남은 180이다
    서는 270이다

"북: {방향.북} (값: {방향.북.값})" 을 출력하라
"동: {방향.동} (값: {방향.동.값})" 을 출력하라
"남: {방향.남} (값: {방향.남.값})" 을 출력하라
"서: {방향.서} (값: {방향.서.값})" 을 출력하라

# 열거형 멤버 속성
"=== 열거형 멤버 속성 ===" 을 출력하라

멤버는 방향.북 이다
"멤버 이름: {멤버.이름}" 을 출력하라
"멤버 값: {멤버.값}" 을 출력하라
"멤버 열거형: {멤버.열거형}" 을 출력하라

# 열거형 비교
"=== 열거형 비교 ===" 을 출력하라

선택은 색상.빨강 이다
만약 선택 이 색상.빨강 과 같으면
    "빨강을 선택했습니다" 를 출력하라

만약 선택 이 색상.파랑 과 다르면
    "파랑이 아닙니다" 를 출력하라

# 문자열 값 열거형
"=== 문자열 값 열거형 ===" 을 출력하라

상태 열거형을 정의하자
    대기는 "PENDING"이다
    진행은 "IN_PROGRESS"이다
    완료는 "COMPLETED"이다
    실패는 "FAILED"이다

"대기 상태: {상태.대기} (값: {상태.대기.값})" 을 출력하라
"진행 상태: {상태.진행} (값: {상태.진행.값})" 을 출력하라
"완료 상태: {상태.완료} (값: {상태.완료.값})" 을 출력하라

# 실용 예제: HTTP 상태 코드
"=== 실용 예제: HTTP 상태 코드 ===" 을 출력하라

HTTP상태 열거형을 정의하자
    OK는 200이다
    생성됨은 201이다
    잘못된요청은 400이다
    권한없음은 401이다
    찾을수없음은 404이다
    서버오류는 500이다

응답상태는 HTTP상태.OK 이다
"응답 상태: {응답상태} (코드: {응답상태.값})" 을 출력하라

만약 응답상태.값 이 200보다 작으면
    "정보 응답" 을 출력하라

만약 응답상태.값 이 200보다 크거나같고 응답상태.값 이 300보다 작으면
    "성공 응답" 을 출력하라

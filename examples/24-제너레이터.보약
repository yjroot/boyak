## 제너레이터 예제 ##

# 기본 제너레이터 (순차적 yield)
"=== 기본 제너레이터 ===" 을 출력하라

숫자들 생성기를 정의하자
    1을 내보내라
    2를 내보내라
    3을 내보내라

숫자목록은 숫자들()이다
각각의 값을 숫자목록에서 반복하자
    "값: {값}" 을 출력하라

# 문자열 제너레이터
"=== 문자열 제너레이터 ===" 을 출력하라

인사말들 생성기를 정의하자
    "안녕하세요"를 내보내라
    "반갑습니다"를 내보내라

인사들은 인사말들()이다
각각의 인사를 인사들에서 반복하자
    인사를 출력하라

# 제너레이터를 목록으로 변환
"=== 제너레이터를 목록으로 변환 ===" 을 출력하라

과일들 생성기를 정의하자
    "사과"를 내보내라
    "배"를 내보내라
    "포도"를 내보내라

과일목록은 목록(과일들())이다
"과일 목록: {과일목록}" 을 출력하라

# 제너레이터 여러 번 사용
"=== 제너레이터 여러 번 사용 ===" 을 출력하라

색상들 생성기를 정의하자
    "빨강"을 내보내라
    "파랑"을 내보내라
    "초록"을 내보내라

"첫 번째 반복:" 을 출력하라
색상1은 색상들()이다
각각의 색을 색상1에서 반복하자
    색을 출력하라

"두 번째 반복:" 을 출력하라
색상2는 색상들()이다
각각의 색을 색상2에서 반복하자
    색을 출력하라
